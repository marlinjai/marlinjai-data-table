import { defineConfig } from '@marlinjai/clearify';

export default defineConfig({
  name: 'Data Table',
  docsDir: './docs',
  hubProject: {
    hubUrl: 'https://docs.lumitra.co',
    hubName: 'ERP Suite',
    description: 'Notion-like database React components & adapters',
    status: 'active',
    icon: '📊',
    tags: ['react', 'components'],
    group: 'Libraries',
  },
});
